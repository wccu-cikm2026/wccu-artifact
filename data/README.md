# Data directory

This repository includes only `cooperbench_mini_sample.jsonl`, a small sample for smoke testing commands.  Full CooperBench-derived inputs should be generated from a local/public CooperBench snapshot or supplied through `COOPERBENCH_INPUT`.

Generated files under `data/$EXP_TAG/` are intentionally ignored by Git.
